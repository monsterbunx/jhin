#!/bin/bash
# jhin/nodejs — instalador Node.js vía tar.xz desde nodejs.org.
# Uso (sourceado): . <(curl -fsSL https://monsterbunx.github.io/jhin/nodejs) [lang] [-v]

LANG_ARG=""
VERBOSE=0
for arg in "$@"; do
  case "$arg" in
    -v|--verbose) VERBOSE=1 ;;
    -h|--help) echo "uso: nodejs [lang] [-v]"; return 0 2>/dev/null || exit 0 ;;
    *) [ -z "$LANG_ARG" ] && LANG_ARG="$arg" ;;
  esac
done
LANG_ARG="${LANG_ARG:-es}"

. <(curl -fsSL https://monsterbunx.github.io/jhin/XT/nodejs) "$LANG_ARG"

say()  { printf '%b\n' "$1"; }
run()  { if [ "$VERBOSE" = "1" ]; then "$@"; else "$@" >/dev/null 2>&1; fi; }

say "$XT_TITLE"

say "$XT_DEPS"
run apt update
run apt upgrade -y
run apt install curl xz-utils -y

rm -rf /usr/local/node

td=$(mktemp -d)
say "$XT_DOWNLOAD"
run curl -L -o "$td/node.tar.xz" https://nodejs.org/dist/v22.15.1/node-v22.15.1-linux-x64.tar.xz

say "$XT_EXTRACT"
run tar -xJf "$td/node.tar.xz" -C "$td"

say "$XT_INSTALL"
mv "$td"/node-v*/ /usr/local/node
rm -rf "$td"

say "$XT_PATH"
echo 'export PATH=/usr/local/node/bin:$PATH' >> ~/.bashrc
export PATH=/usr/local/node/bin:$PATH

say "$XT_DONE"
node -v
npm -v
